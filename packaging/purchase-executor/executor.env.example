# Installed (edited) to /etc/hermes-purchase-executor/executor.env — NON-SECRET.
# Only the deployed Cogitator bridge base URL. No tokens, no card data.
COGITATOR_BRIDGE_URL=https://worker-production-42f3.up.railway.app
